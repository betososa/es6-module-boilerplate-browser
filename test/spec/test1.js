describe('tester', function () {
  it('should execute the tester function', function () {
    tester();
    expect(true).to.equal(true);
  });
  it('should execute the dflt function', function () {
    dflt();
    expect(true).to.equal(true);
  });
});
